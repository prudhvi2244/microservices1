payment:
  url: http://PAYMENT-SERVICE:9002/payment/makePayment

product:
  price: 56000.525

server:
  port: 9001
 
  
spring:  application:    name: ORDER-SERVICE
    
  jpa:    show-sql: true
      h2:    console:      enabled: true
        datasource:    url: jdbc:h2:mem:testdb  config:    import: optional:configserver:http://localhost:9555
      
